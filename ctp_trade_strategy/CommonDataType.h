#pragma once
#include <vector>
#include <string>
#include <map>

using WStringVector = std::vector<std::wstring>;
using NStringVector = std::vector<std::string>;

using StringMap = std::map<std::wstring, std::wstring>;

